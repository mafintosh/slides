const gh = require('./')
const util = require('util')

const auth = {
  token: '89ef6672bcb6e26b583d8e58ea899c726c0153f7'
}

//gh.list(auth, 'nodejs', function (err, list) {
  //console.log(util.inspect(list.map(function (i) { return {
    //name: i.name,
    //desc: i.description,
    //fork: i.fork
  //}})))
//})

gh.listRefs(auth, 'nodejs', 'io.js', function (err, list) {
  console.log(list)
})

//gh.getRef(auth, 'level', 'leveldown', 'refs/heads/bench', function (err, list) {
  //console.log(list)
//})

//var listReleases = gh.repoLister('releases')
//listReleases(auth, 'level', 'leveldown', function (err, list) {
  //console.log(err, list)
//})
